# SkillTwin Dependencies
stripe~=7.0
psycopg2-binary~=2.9  # PostgreSQL driver (para DATABASE_URL en producción)
python-docx~=1.1  # Generación de documentos Word
python-dotenv~=1.0  # Carga de variables desde .env para scripts y utilidades
redis~=5.0  # Cache compartido, sesiones y rate limiting para producción multi-instancia
